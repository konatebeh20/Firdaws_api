# ============================================
# FIRDAWS API - Configuration
# ============================================
# Copiez ce fichier en ".env" et remplissez les valeurs
# ⚠️ Ne committez JAMAIS le fichier .env !
# ============================================

# Environnement (development, production)
FLASK_ENV=development
DEBUG=true

# ========== BASE DE DONNÉES ==========
# Choisissez UNE seule option en décommentant la ligne correspondante

# Option 1 : SQLite (développement local)
LIEN_BASE_DE_DONNEES=sqlite:///firdaws.db

# Option 2 : MySQL avec XAMPP (développement local)
# LIEN_BASE_DE_DONNEES=mysql+pymysql://root:@localhost/firdaws_db

# Option 3 : PostgreSQL (production)
# LIEN_BASE_DE_DONNEES=postgresql://user:password@localhost:5432/firdaws_db

# Option 4 : MySQL avec mot de passe (production)
# LIEN_BASE_DE_DONNEES=mysql+pymysql://user:password@localhost:3306/firdaws_db

# ========== SÉCURITÉ ==========
# Générez des clés avec : python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=votre-cle-secrete-tres-longue
JWT_SECRET_KEY=votre-cle-jwt-tres-longue

# ========== CORS (production uniquement) ==========
# CORS_ORIGINS=https://votresite.com,https://www.votresite.com

# ========== EMAIL ==========
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=votre-email@gmail.com
MAIL_PASSWORD=votre-mot-de-passe-app